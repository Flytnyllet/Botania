using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DpadController : MonoBehaviour
{
    //public static bool _isLeft, _isRight, _isUp, _isDown;
    //private float _lastX, _lastY;

    //private void Update()
    //{
    //    float x = Input.GetAxis("Dpad X");
    //    float y = Input.GetAxis("Dpad Y");

    //    _isLeft = false;
    //    _isRight = false;
    //    _isUp = false;
    //    _isDown = false;

    //    if(_lastX != x)
    //    {
    //        if (x == -1)
    //            _isLeft = true;
    //        else if (x == 1)
    //            _isRight = true;
    //    }
    //    if(_lastY != y)
    //    {
    //        if (y == -1)
    //            _isDown = true;
    //        else if (y == 1)
    //            _isUp = true;
    //    }
    //    _lastX = x;
    //    _lastY = y;
    //}
}
